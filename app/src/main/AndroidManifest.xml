<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cuong.alumni">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignIn" />
        <activity android:name=".SignUp" />
        <activity android:name=".Menu" />
        <activity android:name=".AboutMe" />
        <activity android:name=".Users" />
        <activity android:name=".ModPassword" />
        <activity android:name=".DataModel.Modinfo" />
        <activity android:name=".ModInfo" />
        <activity android:name=".Topics" />
        <activity android:name=".Jobs" />
        <activity android:name=".Insert_Topic" />
        <activity android:name=".TopicComment"></activity>
    </application>

</manifest>